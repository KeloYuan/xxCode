# 🚀 快速开始指南

## 📦 1. 安装

### Windows 系统

```bash
# 1. 进入机器人目录
cd harmonyos-knowledge-bot

# 2. 创建虚拟环境（推荐）
python -m venv venv
venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Linux/Mac 系统

```bash
# 1. 进入机器人目录
cd harmonyos-knowledge-bot

# 2. 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate

# 3. 安装依赖
pip3 install -r requirements.txt
```

## 🎯 2. 基础使用

### 方式 1: 使用启动脚本（推荐）

**Windows:**
```bash
双击运行 start_bot.bat
```

**Linux/Mac:**
```bash
chmod +x start_bot.sh
./start_bot.sh
```

### 方式 2: 命令行运行

```bash
# 自动模式 - 自动搜索和学习所有主题
python knowledge_bot.py --mode auto

# 搜索特定主题
python knowledge_bot.py --mode search --topic "HarmonyOS 动画"

# 爬取指定网页
python knowledge_bot.py --mode crawl --url "https://developer.harmonyos.com/xxx"
```

## ⚙️ 3. 配置说明

编辑 `config.json` 文件：

### 基础配置

```json
{
  "search_topics": [
    "你想学习的主题1",
    "你想学习的主题2"
  ],
  "output_settings": {
    "max_documents_per_run": 5,  // 每次运行最多生成几个文档
    "auto_update_index": true     // 是否自动更新索引
  }
}
```

### 高级配置（AI 功能）

如果想使用 AI 分析功能：

1. 复制 `.env.example` 为 `.env`
2. 填入你的 OpenAI API Key
3. 在 `config.json` 中启用 AI:

```json
{
  "ai_settings": {
    "enabled": true,
    "provider": "openai",
    "model": "gpt-4"
  }
}
```

## 📝 4. 使用示例

### 示例 1: 学习新的动画技巧

```bash
python knowledge_bot.py --mode search --topic "HarmonyOS 粒子动画"
```

机器人会：
1. 🔍 搜索相关内容
2. 📥 爬取网页
3. 🔬 分析质量
4. 📝 生成文档
5. 💾 保存到知识库

### 示例 2: 定时自动学习

编辑 `config.json`:

```json
{
  "schedule": {
    "enabled": true,
    "frequency": "daily",
    "time": "02:00"
  }
}
```

然后运行：

```bash
python knowledge_bot.py --mode auto --schedule daily
```

机器人会每天凌晨 2 点自动运行一次！

### 示例 3: 批量学习多个主题

编辑 `config.json`，添加你想学习的所有主题：

```json
{
  "search_topics": [
    "HarmonyOS 分布式能力",
    "ArkUI 性能优化",
    "鸿蒙应用架构设计",
    "HarmonyOS 数据持久化",
    "鸿蒙跨端开发"
  ]
}
```

运行自动模式：

```bash
python knowledge_bot.py --mode auto
```

## 📊 5. 查看结果

运行后，新生成的文档会保存在：

```
../harmonyos-knowledge/
├── 20251009-harmonyos-particle-animation.md  [新增]
├── 20251009-arkui-performance.md             [新增]
└── changelog.md                              [更新]
```

运行报告示例：

```
=== 学习报告 ===
运行时间: 2025-10-09 14:30:00
搜索主题: 5 个
发现内容: 23 条
有效内容: 15 条
重复内容: 5 条
低质量: 3 条
新增文档: 3 个
更新文档: 2 个
代码示例: 12 个
```

## ⚠️ 6. 注意事项

### 爬取规则
- ✅ 遵守网站的 robots.txt
- ✅ 设置合理的延迟（默认 1-3 秒）
- ✅ 不要频繁爬取同一网站
- ✅ 生成的内容需要人工审核

### 内容质量
机器人会自动过滤：
- ❌ 质量评分 < 0.7 的内容
- ❌ 重复的内容
- ❌ 代码行数太少的内容
- ❌ 内容长度不足的文章

### 性能建议
- 首次运行建议设置 `max_documents_per_run: 3`
- 逐步增加爬取数量
- 定期清理日志文件

## 🐛 7. 常见问题

### Q: 安装依赖失败？
A: 使用国内镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: 搜索不到内容？
A: 检查网络连接，或者尝试：
- 修改 `config.json` 中的 `user_agent`
- 增加 `timeout` 时间
- 使用代理

### Q: 生成的文档质量不高？
A: 调整质量阈值：
```json
{
  "quality_settings": {
    "quality_threshold": 0.8,  // 提高到 0.8
    "min_content_length": 500  // 增加最小长度
  }
}
```

### Q: 想要更多文档？
A: 修改配置：
```json
{
  "output_settings": {
    "max_documents_per_run": 10  // 增加到 10
  }
}
```

## 🎓 8. 下一步

学会基础用法后，可以尝试：

1. **启用 AI 分析** - 获得更好的内容摘要
2. **定时自动运行** - 每天自动学习新知识
3. **自定义主题** - 添加你感兴趣的学习方向
4. **集成到 CI/CD** - 自动化知识库更新

## 💬 9. 获取帮助

```bash
# 查看帮助
python knowledge_bot.py --help

# 查看详细文档
cat README.md
```

---

**🎉 开始你的自动化学习之旅吧！**

